# PHP CircleCI 2.0 configuration file
version: 2
jobs:
  build:
    docker:
      - image: circleci/php

    steps:
      - checkout

      - run: sudo apt update
      # - run: sudo docker-php-ext-install zip

      # quick linting
      - run: php -l *.php

      # deploy
      - add_ssh_keys:
          fingerprints:
            - "e4:e9:75:10:7f:ae:97:81:8a:71:f9:77:51:e4:54:fb"
      - run: scp -r * deployer@ssh.ecobricks.org:/var/www/html/wp-content/themes/ecobricks-wp-theme/
